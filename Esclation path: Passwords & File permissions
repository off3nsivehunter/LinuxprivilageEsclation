stored password, weak passwords,ssh keys etc..

## stored passwords enum:
check history,.bash_history..

check payloadallthethings github check for looting passwords

## weak passwds
check /etc/passwd ,  shadow files permissions

crack passwords of etc/passwd and shadow file using unshadow tool

unshadow etc shadow > file.txt

crack file.txt with john

john --wordlist=rockyou.txt file.txt

## esclating via ssh keys
check cmds via payload allthings repo

if any ssh keys r present get direct access to remotemachine

if ssh key present  >  ssh -i rsa root@<ip>

